<Window x:Class="OpenTrader.Windows.TradeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:OpenTrader.Windows"
        mc:Ignorable="d"
        Title="Trades" Height="450" Width="450">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30" />
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions />
        <ToolBar  DockPanel.Dock="Top" Height="30"  Grid.Row="0" Grid.Column="0" HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch">
            <Button Height="24" Click="AddButton_Click" Margin="1,0,0,0" ToolTip="Add" >
                <Image  Source="/Images/Add.png" />
            </Button>
            <Button Height="24" Click="SaveButton_Click" Margin="1,0,0,0" ToolTip="Save" >
                <Image  Source="/Images/Save.png" />
            </Button>
            <Button Height="24"  Click="DeleteButton_Click" Margin="1,0,0,0" ToolTip="Delete" >
                <Image  Source="/Images/Delete.png" />
            </Button>
        </ToolBar>
        <DataGrid Name="TradeGrid" Grid.Row="1"   Grid.Column="0" AlternatingRowBackground = "LightGray" AlternationCount = "2" PreviewKeyUp="TradeGrid_PreviewKeyUp" SelectionChanged="TradeGrid_SelectionChanged" AutoGenerateColumns="false">
            <DataGrid.Columns>
                <DataGridTemplateColumn Header="Date" Width="1*"  >
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Date, StringFormat=d/M/yyyy}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                    <DataGridTemplateColumn.CellEditingTemplate>
                        <DataTemplate>
                            <DatePicker SelectedDate="{Binding Date}"  />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellEditingTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Price" Binding="{Binding Price}" Width="1*"  ></DataGridTextColumn>
                <DataGridTextColumn Header="Quantity" Binding="{Binding Quantity}" Width="1*"  ></DataGridTextColumn>
                <DataGridTextColumn Header="Strategy" Binding="{Binding Strategy}" Width="1*" ></DataGridTextColumn>
                <DataGridTextColumn Header="Signal" Binding="{Binding Signal}"  Width="1*" ></DataGridTextColumn>
                <DataGridTextColumn Header="Stop" Binding="{Binding Stop}" Width="1*" ></DataGridTextColumn>
                <DataGridTextColumn Header="Target" Binding="{Binding Target}" Width="1*"  ></DataGridTextColumn>
                <DataGridTextColumn Header="Fee" Binding="{Binding Fee}" Width="1*" ></DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
