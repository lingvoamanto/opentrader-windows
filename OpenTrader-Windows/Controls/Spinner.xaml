<UserControl x:Class="OpenTrader.Controls.Spinner"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             Height="32" Width="150">
    <Grid Background="{Binding Path=Background, Mode=OneWay, RelativeSource={RelativeSource TemplatedParent}}" Margin="6,0,0,3">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="10*" />
            <ColumnDefinition Width="62*" />
            <ColumnDefinition Width="16*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="38" />
        </Grid.RowDefinitions>
        <TextBox x:Name="TextBox" 
                 Grid.Row="0"
                 Grid.Column="0"
                 Width="Auto"
                 Margin="0,-4,1,2"
                 VerticalAlignment="Center"
                 IsReadOnly="True" Height="26" Grid.ColumnSpan="2" />
        <Grid x:Name="grid1"
              Grid.Column="2"
              HorizontalAlignment="Center"
              VerticalAlignment="Center" Height="29" Margin="0,-5,-2,0" Width="22">
            <Grid.RowDefinitions>
                <RowDefinition Height="14" />
                <RowDefinition Height="14" />
            </Grid.RowDefinitions>
            <RepeatButton x:Name="Decrement" Grid.Row="0"
                          Grid.Column="1"
                          Width="22"
                          Height="12" FontSize="6" VerticalContentAlignment="Top">
                <RepeatButton.Content>
                    –
                </RepeatButton.Content>
            </RepeatButton>
            <RepeatButton x:Name="Increment" Grid.Row="1"
                          Grid.Column="1"
                          Width="22"
                          Height="12" FontSize="6" VerticalContentAlignment="Top">
                <RepeatButton.Content>
                    +
                </RepeatButton.Content>
            </RepeatButton>
                          
        </Grid>

    </Grid>
</UserControl>
