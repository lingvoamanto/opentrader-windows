<UserControl x:Class="OpenTrader.Controls.EditorControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:OpenTrader.Controls"
             xmlns:avalon="clr-namespace:ICSharpCode.AvalonEdit;assembly=ICSharpCode.AvalonEdit"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="600">
    <Grid Height="Auto" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
        <Grid.RowDefinitions>
            <RowDefinition Height="30" />
            <RowDefinition Height="400*" />
            <RowDefinition Height="5" />
            <RowDefinition Height="50*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions />
        <ToolBar Grid.Row="0" Grid.Column="0" HorizontalAlignment="Stretch">
            <Button Name="CompileButton" ToolTip="Compile">
                <Image Source="../Images/Compile.png" />
            </Button>
            <Button Name="SaveButton" ToolTip="Save">
                <Image Source="../Images/Save.png" />
            </Button>
            <Button Name="SaveAsButton" ToolTip="Save as">
                <Image Source="../Images/Save as.png" />
            </Button>
        </ToolBar>
        <avalonEdit:TextEditor  Grid.Row="1" Grid.Column="0" VerticalAlignment="Stretch" HorizontalAlignment="Stretch"
            xmlns:avalonEdit="http://icsharpcode.net/sharpdevelop/avalonedit"
            Name="AvalonEditor"
            FontFamily="Consolas"
            ShowLineNumbers="true"
            SyntaxHighlighting="C#"
            FontSize="10pt"/>
        <GridSplitter Grid.Row="2" Grid.Column="0" ResizeBehavior="PreviousAndNext" HorizontalAlignment="Stretch"></GridSplitter>
        <TextBox Name="CompileTextBox" Grid.Row="3" Grid.Column="0" ></TextBox>
    </Grid>
</UserControl>
